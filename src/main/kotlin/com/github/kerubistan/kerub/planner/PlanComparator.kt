package com.github.kerubistan.kerub.planner

object PlanComparator : Comparator<Plan> {
	override fun compare(first: Plan, second: Plan): Int {
		//compare two plans by costs (time, IO, risk), violations, outcome

		TODO()
	}
}